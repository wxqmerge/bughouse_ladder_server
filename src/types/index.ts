/**
 * Re-export shared types for frontend components
 * This provides a convenient import path: import { PlayerData } from '../types'
 */

export type { 
  PlayerData, 
  PlayersArray,
  ValidationResult, 
  MatchData, 
  PlayerMatchResult,
  UpdatePlayerGameDataResult,
  ValidationResultResult,
  ProcessResult
} from '../../shared/types';
