import { Router } from 'express';

const router = Router();

// Authentication endpoints are not used in this version
// Admin protection is handled via ADMIN_API_KEY header on /api/admin/* routes

export { router };