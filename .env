# API Server Configuration
# Set this to enable server mode (leave empty for local/standalone mode)
VITE_API_URL=http://localhost:3000

# Package version (automatically set by build, but can be overridden)
# PACKAGE_VERSION=1.0.0
